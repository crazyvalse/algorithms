/*
对称二叉树
给定一个二叉树，检查它是否是镜像对称的。
例如，二叉树 [1,2,2,3,4,4,3] 是对称的。
 */
/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 * 你可以运用递归和迭代两种方法解决这个问题吗？
 */
/**
 * @param {TreeNode} root
 * @return {boolean}
 */
var isSymmetric = function (root) {}

const a = [1, 2, 3, 3, 2, 1]
const b = [1, 2, 3, 3, 2, 1]
console.info(a.reverse().join('') === [1, 2, 3, 3, 2, 1].join(''))
